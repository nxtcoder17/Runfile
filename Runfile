# vim: set ft=yaml:

version: 0.0.1

tasks:
  build:
    cmd: 
      - |+
        echo "building ..."
        go build -o bin/run -ldflags="-s -w" -tags urfave_cli_no_docs cmd/run/main.go
        echo "DONE"

  example:
    cmd: 
      - |+
        run -f ./examples/Runfile cook
